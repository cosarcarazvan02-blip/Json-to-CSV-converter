# Json to CSV converter

A simple Python script that reads a JSON file and converts it to CSV.

## Features
- reads JSON input file
- writes CSV output file
- handles basic validation

## How to run
```bash
python converter.py input.json output.csv
